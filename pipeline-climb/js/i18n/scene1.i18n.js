/* i18n fragment for Scene 1 (Pipeline Climb). Registers this scene's
 * strings into the shared store. English is authoritative; add jp keys
 * as the scene is built out. */
(function () {
  if (!window.I18N || typeof window.I18N.register !== 'function') return;
  window.I18N.register({
    en: {
      'scene1.blurb': 'Tutorial: the ladder, the three levers, the STAGE DIE, and the two endings.',
    },
    jp: {
    },
  });
})();
